/*
for create functional component 
open 'snippets search' (ctrl+alt+r or ctrl+shift+p then open and search for snippets)
then search '_rafce' or 'rafce' and hit enter. 
it will automatically create the functional component

import React from 'react'

const Header = () => {
  return (
    <div>Header</div>
  )
}

export default Header
*/

import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>Thimira Rajitha</h1>
    </header>
  );
};

export default Header;
